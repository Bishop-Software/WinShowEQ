// WinShowEQ client — Rust replacement for MySEQ.exe (C# client).
// See design/RUST_CLIENT_PLAN.md for the full milestone plan.
// C1: data layer, config, filters, and logger. No network or rendering yet.

mod config;
mod data;
mod filters;
mod logger;

fn main() {
    println!("WinShowEQ client — C2 (network) not yet implemented");
}